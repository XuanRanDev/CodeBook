package dev.xuanran.codebook.callback;

import dev.xuanran.codebook.service.CipherStrategy;

public interface CipherStrategyCallback {
    void onCipherStrategyCreated(CipherStrategy cipherStrategy);
}
